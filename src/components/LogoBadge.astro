---
// LogoBadge.astro
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<header class={`fixed top-6 left-6 md:top-10 md:left-10 lg:top-12 lg:left-12 z-50 ${className}`}>
</header>
